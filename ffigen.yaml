# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroServerBindings
description: |
  Bindings for `src/nitro_server.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_server_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro_server.h'
  include-directives:
    - 'src/nitro_server.h'
comments:
  style: any
  length: full
