// Generated by Nitrogen — edit this file to implement NitroServerNative.
//
// This file is a one-time starter: Nitrogen will NOT overwrite it.
// Rename it to e.g. "NitroServerNativeImpl.cpp" and add it to CMakeLists.txt.
//
// Quick-start:
//   1. Implement each TODO method below.
//   2. Add this file to your CMakeLists.txt (target_sources).
//   3. Call nitro_server_register_impl(&myImpl) during plugin/app init.
//   4. Call nitro_server_register_impl(nullptr) in teardown.
//
// Ownership conventions:
//   • Record/variant/tuple RETURNS, **emit_* stream items**, and record/
//     variant CALLBACK arguments you invoke a callback with: pass
//     writer.toNativeBuffer() (or nitro_<Variant>_to_native) — a malloc'd
//     [4B len][payload] block whose ownership transfers to the bridge/Dart.
//     Returning or emitting a non-owning writer.toBuffer() view is wrong:
//     Dart would decode-and-free a live local buffer.
//   • Record/variant PARAMS are non-owning payload views (no length prefix)
//     — copy if you need them after the call.
//   • @HybridStruct RETURNS: the bridge deep-copies every pointer field
//     (String, typed data, nested struct) before Dart sees it, so return
//     views of your own storage (or the argument) and keep ownership of
//     what you return; do not malloc fields for Dart to free.
//   • TypedData RETURNS use NitroCppBuffer{ data, size } where size is in
//     BYTES, not elements (Float32List: count * sizeof(float)). A wrong
//     unit silently truncates the list Dart sees (bytes / elemSize).
//   • @zeroCopy TypedData returns are NOT copied by the bridge: return a
//     malloc'd buffer — ownership transfers, and the bridge frees it (via
//     <lib>_release_typed_data_return) when Dart's view is GC'd. Never
//     return a pointer to a member or stack buffer: it would be free()d.

#include "nitro_server.native.g.h"
#include <stdexcept>

// ── Implementation ───────────────────────────────────────────────────────────

class NitroServerNativeImpl final : public HybridNitroServerNative {
public:
    NitroServerNativeImpl() = default;
    ~NitroServerNativeImpl() override = default;

    // ── Methods ──────────────────────────────────────────────────────────────

    std::string engineVersion() override {
        // TODO: implement engineVersion
        throw std::runtime_error("Not implemented: engineVersion");
        // return "";
    }

    bool supportsTls() override {
        // TODO: implement supportsTls
        throw std::runtime_error("Not implemented: supportsTls");
        // return false;
    }

    void resetNative() override {
        // TODO: implement resetNative
        throw std::runtime_error("Not implemented: resetNative");
    }

    bool supportsBrotli() override {
        // TODO: implement supportsBrotli
        throw std::runtime_error("Not implemented: supportsBrotli");
        // return false;
    }

    NitroCppBuffer brotliEncode(const uint8_t* data, size_t data_length, int64_t quality) override {
        // TODO: implement brotliEncode
        throw std::runtime_error("Not implemented: brotliEncode");
        // return { nullptr, 0 };
    }

    NitroCppBuffer brotliDecode(const uint8_t* data, size_t data_length) override {
        // TODO: implement brotliDecode
        throw std::runtime_error("Not implemented: brotliDecode");
        // return { nullptr, 0 };
    }

    void configureServer(NitroCppBuffer config) override {
        // TODO: implement configureServer
        throw std::runtime_error("Not implemented: configureServer");
    }

    NitroCppBuffer registerRoute(NitroCppBuffer route) override {
        // TODO: implement registerRoute
        throw std::runtime_error("Not implemented: registerRoute");
        // return { nullptr, 0 };
    }

    NitroCppBuffer unregisterRoute(const std::string& method, const std::string& pattern) override {
        // TODO: implement unregisterRoute
        throw std::runtime_error("Not implemented: unregisterRoute");
        // return { nullptr, 0 };
    }

    NitroCppBuffer registerStaticRoute(const std::string& method, const std::string& pattern, int64_t status, NitroCppBuffer headers, const uint8_t* body, size_t body_length) override {
        // TODO: implement registerStaticRoute
        throw std::runtime_error("Not implemented: registerStaticRoute");
        // return { nullptr, 0 };
    }

    NitroCppBuffer start() override {
        // TODO: implement start
        throw std::runtime_error("Not implemented: start");
        // return { nullptr, 0 };
    }

    void stop() override {
        // TODO: implement stop
        throw std::runtime_error("Not implemented: stop");
    }

    void beginDrain() override {
        // TODO: implement beginDrain
        throw std::runtime_error("Not implemented: beginDrain");
    }

    int64_t inFlightRequests() override {
        // TODO: implement inFlightRequests
        throw std::runtime_error("Not implemented: inFlightRequests");
        // return 0;
    }

    int64_t liveConnections() override {
        // TODO: implement liveConnections
        throw std::runtime_error("Not implemented: liveConnections");
        // return 0;
    }

    void respondFile(int64_t requestId, int64_t status, NitroCppBuffer headers, const std::string& path, int64_t offset, int64_t length) override {
        // TODO: implement respondFile
        throw std::runtime_error("Not implemented: respondFile");
    }

    void respond(int64_t requestId, int64_t status, NitroCppBuffer headers, const uint8_t* body, size_t body_length) override {
        // TODO: implement respond
        throw std::runtime_error("Not implemented: respond");
    }

    void ackBody(int64_t requestId, int64_t ackedChunks) override {
        // TODO: implement ackBody
        throw std::runtime_error("Not implemented: ackBody");
    }

    void startStream(int64_t requestId, int64_t status, NitroCppBuffer headers) override {
        // TODO: implement startStream
        throw std::runtime_error("Not implemented: startStream");
    }

    void sendStreamChunk(int64_t requestId, const uint8_t* chunk, size_t chunk_length, bool last) override {
        // TODO: implement sendStreamChunk
        throw std::runtime_error("Not implemented: sendStreamChunk");
    }

    int64_t wsSend(int64_t connectionId, const uint8_t* payload, size_t payload_length, bool binary, bool compressed) override {
        // TODO: implement wsSend
        throw std::runtime_error("Not implemented: wsSend");
        // return 0;
    }

    void wsClose(int64_t connectionId, int64_t code) override {
        // TODO: implement wsClose
        throw std::runtime_error("Not implemented: wsClose");
    }

    // ── Streams ──────────────────────────────────────────────────────────────
    // Call emit_<name>(item) from any thread to push items to Dart.
    // emit_* helpers are defined in the generated bridge.
    // Record/variant items: pass record.toNativeBuffer() — ownership of the
    // heap [4B len][payload] block transfers to the bridge (same convention
    // as record returns). Never emit a non-owning writer.toBuffer() view.
    // Example — start emitting from a background thread:
    //
    //   std::thread([this]{ emit_incomingRequests(/* NitroCppBuffer value */); }).detach();
    //   std::thread([this]{ emit_bodyChunks(/* RawBodyChunk value */); }).detach();
    //   std::thread([this]{ emit_serverEvents(/* NitroCppBuffer value */); }).detach();
    //   std::thread([this]{ emit_wsMessages(/* RawWsMessage value */); }).detach();
};

// ── Registration ─────────────────────────────────────────────────────────────
//
// Create a single instance and register it during plugin/app initialisation:
//
//   static NitroServerNativeImpl g_impl;
//   nitro_server_register_impl(&g_impl);   // in your plugin init
//   nitro_server_register_impl(nullptr);   // in your plugin dispose
//
// On Flutter desktop (Windows / Linux / macOS with NativeImpl.cpp) add the
// registration call to your Flutter plugin's RegisterWithRegistrar:
//
//   void NitroServerNativePlugin::RegisterWithRegistrar(PluginRegistrar* registrar) {
//       static NitroServerNativeImpl impl;
//       nitro_server_register_impl(&impl);
//   }
