// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.6
// Generated from: nitro_server.native.dart
// spec-sha256: 4e70f1c60e9b31ef5d1fad981268dea89e3797699fa874456f1ecb7332261976
package nitro.nitro_server_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.buffer

// --- Enums ---
@Keep
enum class RawServerMethod(val nativeValue: Long) {
  GET(0),
  HEAD(1),
  POST(2),
  PUT(3),
  DELETE(4),
  PATCH(5),
  OPTIONS(6),
  TRACE(7),
  ALL(8),
  CUSTOM(9);

  companion object {
    fun fromNative(v: Long): RawServerMethod = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawServerErrorKind(val nativeValue: Long) {
  NONE(0),
  ALREADYRUNNING(1),
  NOTRUNNING(2),
  BINDFAILED(3),
  TLSERROR(4),
  ROUTENOTFOUND(5),
  HANDLERTIMEOUT(6),
  REQUESTTOOLARGE(7),
  RESPONSETOOLARGE(8),
  IO(9),
  BADREQUEST(10),
  UNKNOWN(11);

  companion object {
    fun fromNative(v: Long): RawServerErrorKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawBodyKind(val nativeValue: Long) {
  DATA(0),
  END(1),
  ERROR(2);

  companion object {
    fun fromNative(v: Long): RawBodyKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class RawServerEventKind(val nativeValue: Long) {
  STARTED(0),
  STOPPED(1),
  HANDLERTIMEOUT(2),
  CLIENTERROR(3),
  NOTICE(4);

  companion object {
    fun fromNative(v: Long): RawServerEventKind = values().first { it.nativeValue == v }
  }
}

// --- Structs ---
@androidx.annotation.Keep
data class RawBodyChunk(val bytes: java.nio.ByteBuffer, val requestId: Long, val kind: Long, val aux: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawBodyChunk {
            val bytes = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val requestId = buf.long
            val kind = buf.long
            val aux = buf.long
            return RawBodyChunk(bytes, requestId, kind, aux)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawBodyChunk {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(requestId)
        writeInt(kind)
        writeInt(aux)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(60)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class RawWsMessage(val payload: java.nio.ByteBuffer, val connectionId: Long, val kind: Long, val aux: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawWsMessage {
            val payload = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val connectionId = buf.long
            val kind = buf.long
            val aux = buf.long
            return RawWsMessage(payload, connectionId, kind, aux)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawWsMessage {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(connectionId)
        writeInt(kind)
        writeInt(aux)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(60)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class RawHeader(val name: String, val value: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawHeader {
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val value = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawHeader(name, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawHeader {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawHeader = RawHeader(
                name = map["name"] as String,
                value = map["value"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(72) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(name)
        writeString(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawHeader> support ---
    fun toJson(): Map<String, Any?> = mapOf("name" to name, "value" to value)

}

@androidx.annotation.Keep
data class RawIncomingBatch(val requests: List<RawIncomingRequest>) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawIncomingBatch {
            val requests = (0 until buf.int).map { RawIncomingRequest.decodeFrom(buf) }
            return RawIncomingBatch(requests)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawIncomingBatch {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawIncomingBatch = RawIncomingBatch(
                requests = @Suppress("UNCHECKED_CAST") (map["requests"] as List<RawIncomingRequest>)
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(36) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt32(requests.size)
        requests.forEach { e -> e.writeFieldsTo(out, buf) }
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawIncomingBatch> support ---
    fun toJson(): Map<String, Any?> = mapOf("requests" to requests)

}

@androidx.annotation.Keep
data class RawRouteParam(val name: String, val value: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawRouteParam {
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val value = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawRouteParam(name, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawRouteParam {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawRouteParam = RawRouteParam(
                name = map["name"] as String,
                value = map["value"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(72) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(name)
        writeString(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawRouteParam> support ---
    fun toJson(): Map<String, Any?> = mapOf("name" to name, "value" to value)

}

@androidx.annotation.Keep
data class RawTlsConfig(val certPem: String, val keyPem: String, val certFile: String, val keyFile: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawTlsConfig {
            val certPem = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val keyPem = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val certFile = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val keyFile = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawTlsConfig(certPem, keyPem, certFile, keyFile)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawTlsConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawTlsConfig = RawTlsConfig(
                certPem = map["certPem"] as String,
                keyPem = map["keyPem"] as String,
                certFile = map["certFile"] as String,
                keyFile = map["keyFile"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(144) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(certPem)
        writeString(keyPem)
        writeString(certFile)
        writeString(keyFile)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawTlsConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("certPem" to certPem, "keyPem" to keyPem, "certFile" to certFile, "keyFile" to keyFile)

}

@androidx.annotation.Keep
data class RawServerConfig(val host: String, val port: Long, val backlog: Long, val maxBodyBytes: Long, val defaultTimeoutMs: Long, val keepAliveTimeoutMs: Long, val maxRequestsPerConn: Long, val workerThreads: Long, val maxConnections: Long, val maxConnectionsPerIp: Long, val headerTimeoutMs: Long, val writeTimeoutMs: Long, val wsMaxBufferBytes: Long, val wsCompression: Boolean, val tls: RawTlsConfig) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawServerConfig {
            val host = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val port = buf.long
            val backlog = buf.long
            val maxBodyBytes = buf.long
            val defaultTimeoutMs = buf.long
            val keepAliveTimeoutMs = buf.long
            val maxRequestsPerConn = buf.long
            val workerThreads = buf.long
            val maxConnections = buf.long
            val maxConnectionsPerIp = buf.long
            val headerTimeoutMs = buf.long
            val writeTimeoutMs = buf.long
            val wsMaxBufferBytes = buf.long
            val wsCompression = (buf.get().toInt() != 0)
            val tls = RawTlsConfig.decodeFrom(buf)
            return RawServerConfig(host, port, backlog, maxBodyBytes, defaultTimeoutMs, keepAliveTimeoutMs, maxRequestsPerConn, workerThreads, maxConnections, maxConnectionsPerIp, headerTimeoutMs, writeTimeoutMs, wsMaxBufferBytes, wsCompression, tls)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawServerConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawServerConfig = RawServerConfig(
                host = map["host"] as String,
                port = (map["port"] as Number).toLong(),
                backlog = (map["backlog"] as Number).toLong(),
                maxBodyBytes = (map["maxBodyBytes"] as Number).toLong(),
                defaultTimeoutMs = (map["defaultTimeoutMs"] as Number).toLong(),
                keepAliveTimeoutMs = (map["keepAliveTimeoutMs"] as Number).toLong(),
                maxRequestsPerConn = (map["maxRequestsPerConn"] as Number).toLong(),
                workerThreads = (map["workerThreads"] as Number).toLong(),
                maxConnections = (map["maxConnections"] as Number).toLong(),
                maxConnectionsPerIp = (map["maxConnectionsPerIp"] as Number).toLong(),
                headerTimeoutMs = (map["headerTimeoutMs"] as Number).toLong(),
                writeTimeoutMs = (map["writeTimeoutMs"] as Number).toLong(),
                wsMaxBufferBytes = (map["wsMaxBufferBytes"] as Number).toLong(),
                wsCompression = map["wsCompression"] as Boolean,
                tls = RawTlsConfig.fromJson(@Suppress("UNCHECKED_CAST") (map["tls"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(165) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(host)
        writeInt(port.toLong())
        writeInt(backlog.toLong())
        writeInt(maxBodyBytes.toLong())
        writeInt(defaultTimeoutMs.toLong())
        writeInt(keepAliveTimeoutMs.toLong())
        writeInt(maxRequestsPerConn.toLong())
        writeInt(workerThreads.toLong())
        writeInt(maxConnections.toLong())
        writeInt(maxConnectionsPerIp.toLong())
        writeInt(headerTimeoutMs.toLong())
        writeInt(writeTimeoutMs.toLong())
        writeInt(wsMaxBufferBytes.toLong())
        writeBool(wsCompression)
        tls.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawServerConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("host" to host, "port" to port, "backlog" to backlog, "maxBodyBytes" to maxBodyBytes, "defaultTimeoutMs" to defaultTimeoutMs, "keepAliveTimeoutMs" to keepAliveTimeoutMs, "maxRequestsPerConn" to maxRequestsPerConn, "workerThreads" to workerThreads, "maxConnections" to maxConnections, "maxConnectionsPerIp" to maxConnectionsPerIp, "headerTimeoutMs" to headerTimeoutMs, "writeTimeoutMs" to writeTimeoutMs, "wsMaxBufferBytes" to wsMaxBufferBytes, "wsCompression" to wsCompression, "tls" to tls.toJson())

}

@androidx.annotation.Keep
data class RawRouteConfig(val method: RawServerMethod, val customMethod: String, val pattern: String, val timeoutMs: Long, val isWebSocket: Boolean, val streamBody: Boolean, val maxBodyBytes: Long, val wsProtocols: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawRouteConfig {
            val method = RawServerMethod.fromNative(buf.long)
            val customMethod = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pattern = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val timeoutMs = buf.long
            val isWebSocket = (buf.get().toInt() != 0)
            val streamBody = (buf.get().toInt() != 0)
            val maxBodyBytes = buf.long
            val wsProtocols = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawRouteConfig(method, customMethod, pattern, timeoutMs, isWebSocket, streamBody, maxBodyBytes, wsProtocols)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawRouteConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawRouteConfig = RawRouteConfig(
                method = RawServerMethod.fromNative(map["method"] as Long),
                customMethod = map["customMethod"] as String,
                pattern = map["pattern"] as String,
                timeoutMs = (map["timeoutMs"] as Number).toLong(),
                isWebSocket = map["isWebSocket"] as Boolean,
                streamBody = map["streamBody"] as Boolean,
                maxBodyBytes = (map["maxBodyBytes"] as Number).toLong(),
                wsProtocols = map["wsProtocols"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(134) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(method.nativeValue)
        writeString(customMethod)
        writeString(pattern)
        writeInt(timeoutMs.toLong())
        writeBool(isWebSocket)
        writeBool(streamBody)
        writeInt(maxBodyBytes.toLong())
        writeString(wsProtocols)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawRouteConfig> support ---
    fun toJson(): Map<String, Any?> = mapOf("method" to method.nativeValue, "customMethod" to customMethod, "pattern" to pattern, "timeoutMs" to timeoutMs, "isWebSocket" to isWebSocket, "streamBody" to streamBody, "maxBodyBytes" to maxBodyBytes, "wsProtocols" to wsProtocols)

}

@androidx.annotation.Keep
data class RawServerStatus(val errorKind: RawServerErrorKind, val errorMessage: String, val boundPort: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawServerStatus {
            val errorKind = RawServerErrorKind.fromNative(buf.long)
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val boundPort = buf.long
            return RawServerStatus(errorKind, errorMessage, boundPort)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawServerStatus {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawServerStatus = RawServerStatus(
                errorKind = RawServerErrorKind.fromNative(map["errorKind"] as Long),
                errorMessage = map["errorMessage"] as String,
                boundPort = (map["boundPort"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(52) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(errorKind.nativeValue)
        writeString(errorMessage)
        writeInt(boundPort.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawServerStatus> support ---
    fun toJson(): Map<String, Any?> = mapOf("errorKind" to errorKind.nativeValue, "errorMessage" to errorMessage, "boundPort" to boundPort)

}

@androidx.annotation.Keep
data class RawIncomingRequest(val requestId: Long, val method: RawServerMethod, val customMethod: String, val path: String, val query: String, val packedHeaders: String, val contentLength: Long, val hasBody: Boolean, val bodyComplete: Boolean, val routePattern: String, val params: List<RawRouteParam>) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawIncomingRequest {
            val requestId = buf.long
            val method = RawServerMethod.fromNative(buf.long)
            val customMethod = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val path = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val query = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val packedHeaders = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val contentLength = buf.long
            val hasBody = (buf.get().toInt() != 0)
            val bodyComplete = (buf.get().toInt() != 0)
            val routePattern = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val params = (0 until buf.int).map { RawRouteParam.decodeFrom(buf) }
            return RawIncomingRequest(requestId, method, customMethod, path, query, packedHeaders, contentLength, hasBody, bodyComplete, routePattern, params)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawIncomingRequest {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawIncomingRequest = RawIncomingRequest(
                requestId = (map["requestId"] as Number).toLong(),
                method = RawServerMethod.fromNative(map["method"] as Long),
                customMethod = map["customMethod"] as String,
                path = map["path"] as String,
                query = map["query"] as String,
                packedHeaders = map["packedHeaders"] as String,
                contentLength = (map["contentLength"] as Number).toLong(),
                hasBody = map["hasBody"] as Boolean,
                bodyComplete = map["bodyComplete"] as Boolean,
                routePattern = map["routePattern"] as String,
                params = @Suppress("UNCHECKED_CAST") (map["params"] as List<RawRouteParam>)
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(242) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(requestId.toLong())
        writeInt(method.nativeValue)
        writeString(customMethod)
        writeString(path)
        writeString(query)
        writeString(packedHeaders)
        writeInt(contentLength.toLong())
        writeBool(hasBody)
        writeBool(bodyComplete)
        writeString(routePattern)
        writeInt32(params.size)
        params.forEach { e -> e.writeFieldsTo(out, buf) }
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawIncomingRequest> support ---
    fun toJson(): Map<String, Any?> = mapOf("requestId" to requestId, "method" to method.nativeValue, "customMethod" to customMethod, "path" to path, "query" to query, "packedHeaders" to packedHeaders, "contentLength" to contentLength, "hasBody" to hasBody, "bodyComplete" to bodyComplete, "routePattern" to routePattern, "params" to params)

}

@androidx.annotation.Keep
data class RawServerEvent(val kind: Long, val requestId: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawServerEvent {
            val kind = buf.long
            val requestId = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return RawServerEvent(kind, requestId, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawServerEvent {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RawServerEvent = RawServerEvent(
                kind = (map["kind"] as Number).toLong(),
                requestId = (map["requestId"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(52) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(kind.toLong())
        writeInt(requestId.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RawServerEvent> support ---
    fun toJson(): Map<String, Any?> = mapOf("kind" to kind, "requestId" to requestId, "message" to message)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroServerNative] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroServerNativeSpec {
    val applicationContext: Context get() = NitroServerNativeJniBridge.applicationContext
    val activity: Activity? get() = NitroServerNativeJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_server.native.dart:404
    fun engineVersion(): String
    // source: nitro_server.native.dart:406
    fun supportsTls(): Boolean
    // source: nitro_server.native.dart:410
    fun resetNative(): Unit
    // source: nitro_server.native.dart:414
    fun supportsBrotli(): Boolean
    // source: nitro_server.native.dart:419
    fun brotliEncode(data: java.nio.ByteBuffer, quality: Long): java.nio.ByteBuffer
    // source: nitro_server.native.dart:425
    fun brotliDecode(data: java.nio.ByteBuffer): java.nio.ByteBuffer
    // source: nitro_server.native.dart:431
    fun configureServer(config: RawServerConfig): Unit
    // source: nitro_server.native.dart:433
    fun registerRoute(route: RawRouteConfig): RawServerStatus
    // source: nitro_server.native.dart:438
    fun unregisterRoute(method: String, pattern: String): RawServerStatus
    // source: nitro_server.native.dart:447
    fun registerStaticRoute(method: String, pattern: String, status: Long, headers: List<RawHeader>, body: java.nio.ByteBuffer): RawServerStatus
    // source: nitro_server.native.dart:457
    fun start(): RawServerStatus
    // source: nitro_server.native.dart:459
    fun stop(): Unit
    // source: nitro_server.native.dart:465
    fun beginDrain(): Unit
    // source: nitro_server.native.dart:468
    fun inFlightRequests(): Long
    // source: nitro_server.native.dart:473
    fun liveConnections(): Long
    // source: nitro_server.native.dart:481
    fun respondFile(requestId: Long, status: Long, headers: List<RawHeader>, path: String, offset: Long, length: Long): Unit
    // source: nitro_server.native.dart:494
    fun respond(requestId: Long, status: Long, headers: List<RawHeader>, body: java.nio.ByteBuffer): Unit
    // source: nitro_server.native.dart:511
    fun ackBody(requestId: Long, ackedChunks: Long): Unit
    // source: nitro_server.native.dart:526
    fun startStream(requestId: Long, status: Long, headers: List<RawHeader>): Unit
    // source: nitro_server.native.dart:529
    fun sendStreamChunk(requestId: Long, chunk: java.nio.ByteBuffer, last: Boolean): Unit
    // source: nitro_server.native.dart:548
    fun wsSend(connectionId: Long, payload: java.nio.ByteBuffer, binary: Boolean, compressed: Boolean): Long
    // source: nitro_server.native.dart:556
    fun wsClose(connectionId: Long, code: Long): Unit
    val incomingRequests: Flow<RawIncomingBatch>
    val bodyChunks: Flow<RawBodyChunk>
    val serverEvents: Flow<RawServerEvent>
    val wsMessages: Flow<RawWsMessage>
}

@Keep
object NitroServerNativeJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroServerNativeSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroServerNativeSpec)? = null

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    private val _zeroCopyPool = java.util.concurrent.ConcurrentHashMap<Int, java.util.concurrent.ArrayBlockingQueue<java.nio.ByteBuffer>>()
    private const val _kZeroCopyPoolPerSize = 8

    /// Borrows a direct buffer of exactly [size] bytes, reusing a released one
    /// when available. Use this instead of allocateDirect() for @zeroCopy
    /// returns. Bounded and non-blocking — it never makes a caller wait.
    @JvmStatic fun acquireZeroCopyBuffer(size: Int): java.nio.ByteBuffer {
        val reused = _zeroCopyPool[size]?.poll()
        if (reused != null) { reused.clear(); return reused }
        return java.nio.ByteBuffer.allocateDirect(size).order(java.nio.ByteOrder.nativeOrder())
    }

    /// Called from the native release path once Dart has released the view.
    /// offer() drops the buffer when the pool is full, so memory stays bounded.
    @JvmStatic fun releaseZeroCopyBuffer(buf: java.nio.ByteBuffer) {
        if (!buf.isDirect) return
        _zeroCopyPool.computeIfAbsent(buf.capacity()) {
            java.util.concurrent.ArrayBlockingQueue(_kZeroCopyPoolPerSize)
        }.offer(buf)
    }

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroServerNativeSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroServerNative: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // source: nitro_server.native.dart:404
    @JvmStatic fun engineVersion_call(instanceId: Long): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.engineVersion()
    }
    // source: nitro_server.native.dart:406
    @JvmStatic fun supportsTls_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.supportsTls()
    }
    // source: nitro_server.native.dart:410
    @JvmStatic fun resetNative_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.resetNative()
    }
    // source: nitro_server.native.dart:414
    @JvmStatic fun supportsBrotli_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.supportsBrotli()
    }
    // source: nitro_server.native.dart:419
    @JvmStatic fun brotliEncode_call(instanceId: Long, data: java.nio.ByteBuffer, quality: Long): java.nio.ByteBuffer {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.brotliEncode(data, quality)
    }
    // source: nitro_server.native.dart:425
    @JvmStatic fun brotliDecode_call(instanceId: Long, data: java.nio.ByteBuffer): java.nio.ByteBuffer {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.brotliDecode(data)
    }
    // source: nitro_server.native.dart:431
    @JvmStatic fun configureServer_call(instanceId: Long, config: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val configBuf = java.nio.ByteBuffer.wrap(config).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        configBuf.getInt() // skip Dart 4-byte outer length prefix
        val configDecoded = RawServerConfig.decodeFrom(configBuf)
        impl.configureServer(configDecoded)
    }
    // source: nitro_server.native.dart:433
    @JvmStatic fun registerRoute_call(instanceId: Long, route: ByteArray): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val routeBuf = java.nio.ByteBuffer.wrap(route).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        routeBuf.getInt() // skip Dart 4-byte outer length prefix
        val routeDecoded = RawRouteConfig.decodeFrom(routeBuf)
        val result = impl.registerRoute(routeDecoded)
        return result.encode()
    }
    // source: nitro_server.native.dart:438
    @JvmStatic fun unregisterRoute_call(instanceId: Long, method: String, pattern: String): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val result = impl.unregisterRoute(method, pattern)
        return result.encode()
    }
    // source: nitro_server.native.dart:447
    @JvmStatic fun registerStaticRoute_call(instanceId: Long, method: String, pattern: String, status: Long, headers: ByteArray, body: java.nio.ByteBuffer): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val headersBuf = java.nio.ByteBuffer.wrap(headers).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        headersBuf.getInt() // skip outer length
        val headersCount = headersBuf.getInt()
        val headersOffsets = LongArray(headersCount) { headersBuf.getLong() }
        val headersDecoded = mutableListOf<RawHeader>()
        for (headersOffset in headersOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(headers, 4 + headersOffset.toInt(), headers.size - 4 - headersOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            headersDecoded.add(RawHeader.decodeFrom(itemBuf))
        }
        val result = impl.registerStaticRoute(method, pattern, status, headersDecoded, body)
        return result.encode()
    }
    // source: nitro_server.native.dart:457
    @JvmStatic fun start_call(instanceId: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val result = impl.start()
        return result.encode()
    }
    // source: nitro_server.native.dart:459
    @JvmStatic fun stop_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.stop()
    }
    // source: nitro_server.native.dart:465
    @JvmStatic fun beginDrain_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.beginDrain()
    }
    // source: nitro_server.native.dart:468
    @JvmStatic fun inFlightRequests_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.inFlightRequests()
    }
    // source: nitro_server.native.dart:473
    @JvmStatic fun liveConnections_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.liveConnections()
    }
    // source: nitro_server.native.dart:481
    @JvmStatic fun respondFile_call(instanceId: Long, requestId: Long, status: Long, headers: ByteArray, path: String, offset: Long, length: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val headersBuf = java.nio.ByteBuffer.wrap(headers).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        headersBuf.getInt() // skip outer length
        val headersCount = headersBuf.getInt()
        val headersOffsets = LongArray(headersCount) { headersBuf.getLong() }
        val headersDecoded = mutableListOf<RawHeader>()
        for (headersOffset in headersOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(headers, 4 + headersOffset.toInt(), headers.size - 4 - headersOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            headersDecoded.add(RawHeader.decodeFrom(itemBuf))
        }
        impl.respondFile(requestId, status, headersDecoded, path, offset, length)
    }
    // source: nitro_server.native.dart:494
    @JvmStatic fun respond_call(instanceId: Long, requestId: Long, status: Long, headers: ByteArray, body: java.nio.ByteBuffer): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val headersBuf = java.nio.ByteBuffer.wrap(headers).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        headersBuf.getInt() // skip outer length
        val headersCount = headersBuf.getInt()
        val headersOffsets = LongArray(headersCount) { headersBuf.getLong() }
        val headersDecoded = mutableListOf<RawHeader>()
        for (headersOffset in headersOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(headers, 4 + headersOffset.toInt(), headers.size - 4 - headersOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            headersDecoded.add(RawHeader.decodeFrom(itemBuf))
        }
        impl.respond(requestId, status, headersDecoded, body)
    }
    // source: nitro_server.native.dart:511
    @JvmStatic fun ackBody_call(instanceId: Long, requestId: Long, ackedChunks: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.ackBody(requestId, ackedChunks)
    }
    // source: nitro_server.native.dart:526
    @JvmStatic fun startStream_call(instanceId: Long, requestId: Long, status: Long, headers: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        val headersBuf = java.nio.ByteBuffer.wrap(headers).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        headersBuf.getInt() // skip outer length
        val headersCount = headersBuf.getInt()
        val headersOffsets = LongArray(headersCount) { headersBuf.getLong() }
        val headersDecoded = mutableListOf<RawHeader>()
        for (headersOffset in headersOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(headers, 4 + headersOffset.toInt(), headers.size - 4 - headersOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            headersDecoded.add(RawHeader.decodeFrom(itemBuf))
        }
        impl.startStream(requestId, status, headersDecoded)
    }
    // source: nitro_server.native.dart:529
    @JvmStatic fun sendStreamChunk_call(instanceId: Long, requestId: Long, chunk: java.nio.ByteBuffer, last: Boolean): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.sendStreamChunk(requestId, chunk, last)
    }
    // source: nitro_server.native.dart:548
    @JvmStatic fun wsSend_call(instanceId: Long, connectionId: Long, payload: java.nio.ByteBuffer, binary: Boolean, compressed: Boolean): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        return impl.wsSend(connectionId, payload, binary, compressed)
    }
    // source: nitro_server.native.dart:556
    @JvmStatic fun wsClose_call(instanceId: Long, connectionId: Long, code: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroServerNative instance $instanceId not registered")
        impl.wsClose(connectionId, code)
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_incomingRequests(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_server_register_incoming_requests_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("incomingRequests", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.incomingRequests
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_incomingRequests(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("incomingRequests", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_server_release_incoming_requests_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("incomingRequests", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_bodyChunks(dartPort: Long, item: RawBodyChunk): Boolean

    @JvmStatic fun nitro_server_register_body_chunks_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("bodyChunks", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.bodyChunks
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_bodyChunks(dartPort, item)) {
                        _streamJobs.remove(Pair("bodyChunks", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_server_release_body_chunks_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("bodyChunks", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_serverEvents(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_server_register_server_events_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("serverEvents", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.serverEvents
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_serverEvents(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("serverEvents", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_server_release_server_events_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("serverEvents", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_wsMessages(dartPort: Long, item: RawWsMessage): Boolean

    @JvmStatic fun nitro_server_register_ws_messages_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("wsMessages", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.wsMessages
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_wsMessages(dartPort, item)) {
                        _streamJobs.remove(Pair("wsMessages", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_server_release_ws_messages_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("wsMessages", dartPort))?.cancel()
    }
}
